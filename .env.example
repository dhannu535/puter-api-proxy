# Get your token from https://puter.com/dashboard
PUTER_AUTH_TOKEN=your-puter-token-here

# Port for the proxy server (default: 3800)
PORT=3800

# API key for authenticating requests to YOUR proxy (set to "none" to disable)
API_KEY=sk-puter-proxy

# Dashboard (set to "false" to disable)
DASHBOARD=true
